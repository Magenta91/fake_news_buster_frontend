# Fake News Buster — Frontend

Next.js frontend displaying articles and verification results.

Run locally

1. Install:

```bash
npm install
```

2. Set environment variable `NEXT_PUBLIC_API_URL` to point to backend API.

3. Run:

```bash
npm run dev
```

Deployment

- Vercel: set `NEXT_PUBLIC_API_URL` in project settings.
